export function cleanInboxBody(value?: string) { return (value ?? "").replace(/<style[\s\S]*?<\/style>|@import[^;]+;|(?:https?:\/\/|www\.)\S+/gi, "").replace(/(?:se désinscrire|unsubscribe|mentions légales)[\s\S]*/i, "").replace(/\n\s*\n\s*\n+/g, "\n\n").replace(/[ \t]{2,}/g, " ").trim(); }
export type Parsed1001 = { name?:string; email?:string; phone?:string; eventType?:string; eventDate?:string; venue?:string; guests?:number; budgetPerPerson?:number; cateringMode?:string; options?:string[] };
export function parse1001Request(sender?:string, subject?:string, body?:string): Parsed1001 | undefined { const text=body??""; if(!/@1001traiteurs\.com$/i.test(sender??"") || !/nouvelle demande/i.test(subject??"")) return; const get=(label:string)=>text.match(new RegExp(`${label}\\s*:\\s*([^\\n]+)`,"i"))?.[1]?.trim(); const first=get("Prénom"), last=get("Nom"); const date=get("Date")?.match(/(\d{2}\/\d{2}\/\d{4})/)?.[1]; return { name:[first,last].filter(Boolean).join(" ")||undefined,email:get("Adresse email"),phone:get("Téléphone mobile"),eventType:get("Type d'événement"),eventDate:date,venue:get("Ville"),guests:Number(get("Nombre d'invités"))||undefined,budgetPerPerson:Number(text.match(/budget de\s*(\d+(?:[,.]\d+)?)\s*€?\s*par personne/i)?.[1]?.replace(",","."))||undefined,cateringMode:get("Mode de restauration"),options:get("Option\\(s\\) de service")?.split(/\s*-\s*|;/).filter(Boolean) }; }
