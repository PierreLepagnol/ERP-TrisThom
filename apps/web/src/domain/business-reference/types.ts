export type DataStatus = "real" | "estimated" | "to_test";
export type SaleUnit = "piece" | "portion" | "tray" | "board" | "liter" | "person" | "package";
export type ReferenceCatalogItem = { id: string; name: string; category: string; portionLabel: string; saleUnit: SaleUnit; materialCostPerUnitCents: number; suggestedSalePriceHtCents: number; estimatedMaterialMarginRate: number; compatibleOfferFamilyIds: string[]; productionBaseIds: string[]; temperature: "cold" | "hot" | "sweet" | "other" | string; seasons: string[]; diets: string[]; allergens: string[]; minimumQuantity?: number; productionMultiple?: number; difficulty: "low" | "medium" | "high" | "unknown" | string; activeTimeMinutes?: number; transportFragility: "low" | "medium" | "high" | "unknown" | string; dataStatus: DataStatus; active: boolean; notes?: string };
export type OfferFamily = { id: string; name: string; customerNeed: string; configuration: string; duration: string; budget: string; structure: string; constraints: string; alternatives: string[]; priority: string };
export type ProductionBase = { id: string; name: string; compatiblePreparations: string[]; commercialFamilies: string[]; allergens: string[]; diets: string[]; seasons: string[]; shareable: boolean; priority: string; notes: string };
export type PricingPolicy = { format: string; publicPriceTtc: number; structure: string; positioning: string; reason: string; targetMaterialCost: string; rule: string; excludedOptions: string[] };
export type BusinessReference = { catalogItems: readonly ReferenceCatalogItem[]; offerFamilies: readonly OfferFamily[]; productionBases: readonly ProductionBase[]; businessRules: readonly unknown[]; testScenarios: readonly unknown[]; offerCompositions: readonly unknown[]; services: readonly unknown[]; pricingPolicies: readonly PricingPolicy[]; articleEstimationRules: readonly unknown[] };
export type ProductionLoad = { activeMinutes: number; referenceCount: number; difficultyScore: number; fragileItemCount: number };
